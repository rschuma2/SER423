The folder: StudentCourseSqliteApp contains an AndroidStudio app that demonstrates Sqlite3
databases distributed via the bundle.

The folder: StudentSqlite3 contains command-line
initialization and readme file demonstrating how Sqlite3 databases can be created and
initialized prior to app distribution.

Opening this folder (current directory) in Android
Studio yields an empty project.
